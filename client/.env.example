# Dist folder not required in the repo: it's produced when you run the frontend build.
# Leave blank for local development: Vite proxies /api to localhost:5000.
# For a deployed frontend, set this to the public API origin, e.g.
# VITE_API_URL=https://api.example.com/api
VITE_API_URL=http://localhost:5173
